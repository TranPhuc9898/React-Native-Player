export interface IListFeatureItem {
  title: string;
  description: string;
}
export interface IListFeatureDish {
  id: number;
  name: string;
  image: any;
  category: string;
  label: string;
  price: string;
  featured: boolean;
  description: string;
}
